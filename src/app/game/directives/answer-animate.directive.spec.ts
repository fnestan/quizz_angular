import { AnswerAnimateDirective } from './answer-animate.directive';

describe('AnswerAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
